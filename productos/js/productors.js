// alerta
const alertButton = document.querySelector(".button--alert");
alertButton.addEventListener("click", function (event) {
	event.preventDefault();
	alert("¿Seguro que desea continuar?");
});

document
	.querySelector(".button--confirm")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--ok")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--ready")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--ya")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--listo")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--listo2")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--listo3")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});

  document
	.querySelector(".button--listo4")
	.addEventListener("click", function (event) {
		event.preventDefault();
		const userConfirms = confirm("¿Seguro que desea continuar?");
		console.log(userConfirms);
	});